# Graph algorithm implmentations in pure C

The goal was to measure the JNI overhead in the Java benchmarks (using graphblas-java-native).

## Usage
* build with `gcc triangleCount.c -lgraphblas`
* run `./a.out {pathToCsv}`
